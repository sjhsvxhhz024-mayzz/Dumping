Eclips Oxide — external, обновление (возврат к внешнему)

1) ФИКС резолвера классов (src/main.cpp):
   - было: искали в памяти указатель, равный найденной копии строки-имени;
     klass внутри ссылается на КАНОНИЧНУЮ копию (блоб метадаты) -> не матчилось.
   - стало: ox_scanClassByName() — перебираем слоты, разыменовываем
     klass+0x10 и сравниваем СОДЕРЖИМОЕ имени. Работает независимо
     от того, на какую копию строки ссылается класс.
   -> это чинит лог "литералы=10, но класс не найден".

2) ВСЕ RVA из дампа:
   - include/oxide_rva.h  — 1777 method-RVA целевых классов (base+RVA).
   - OXIDE_all_rva.txt    — читаемый список: поля (оффсеты) + методы (RVA).
   - dump.cs              — свежий полный дамп (29366 типов).

Ключевые (сверены): PlayerManager.Update 0x5364E84, Awake 0x536449C;
WeaponRecoil.dFM 0x5B2965C, dFH 0x5B295A8; FPHitscan.Update 0x52D33C8;
Camera.get_main 0xA9E02CC, get_main_Injected 0xA9E032C, WorldToScreenPoint 0xA9DF820.

3) ФИКС ESP (базис камеры):
   - было: pitch читался как float с mouseLook+0x60, но в дампе 0x60 = Vector2 kqP
     (обфусцировано) -> мусор, боксы «плыли» по вертикали при взгляде вверх/вниз.
   - стало: базис камеры строится из ПОЛНОГО мирового кватерниона m_LookRoot
     (yaw+pitch сразу). Старый pitch-оффсет — только fallback. Автодетект
     наклона (forward.y). Переключатель в меню Camera: "Use LookRoot full pitch".
